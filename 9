def power(x, y):
    if y == 0:
        return 1
    if y==1:
        return x
        
    half = power(x,y //2)
    if y //2 == 0:
        return half * half
    else:
        return half * half * x
x, y = map(int, input().split())

print(power(x,y))
